# Web4 3D Architecture Viewer

Interactive Three.js visualization of a Web4 identity + blockchain system.

## Layers

- Identity Layer (.p12 keystores)
- FastAPI KMS Server
- Wallet Layer
- Node Layer
- P2P Blockchain Layer

## Run

Just open `index.html` in a browser.

Or use a local server:

```bash
npx serve
